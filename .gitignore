__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.so

# Local / packaging leftovers
.avbtool-dist/
dist/
build/
build_logs/
logs/
*.spec

# Key-material test/demo fixtures: excluded from source builds to keep
# GitHub push protection (secret scanning) happy. Not needed to build the
# openssl CLI or libcrypto.
openssl/test/
openssl/apps/*.pem
openssl/demos/

# Key-material test/demo fixtures: excluded from source builds to keep
# GitHub push protection (secret scanning) happy. Not needed to build the
# openssl CLI or libcrypto.
openssl/test/
openssl/apps/*.pem
openssl/demos/
# Nested project metadata that would be picked up by the runner
openssl/.github/
fec/.github/
